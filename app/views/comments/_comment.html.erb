<%= comment.user_id %> <br>
<%= comment.body %> <br>
<%= time_ago_in_words(comment.created_at) %> <br>

<% if current_user %>
    <% if comment.user_id == current_user.id %>
        <%= link_to 'Delete', [comment.movie,comment], method: :delete, data: {confirm: "Are you sure"}%></p> 
    <% end %>
<% end %>